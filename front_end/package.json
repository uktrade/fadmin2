{
  "name": "fido",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production react-scripts build",
    "start": "BROWSER=none NODE_ENV=development PUBLIC_PATH=http://localhost:3000/ react-scripts start",
    "bdd": "BROWSER=none NODE_ENV=development PUBLIC_PATH=http://host.docker.internal:3000/ react-scripts start"
  },
  "dependencies": {
    "react-scripts": "^1.0.17"
  }
}
